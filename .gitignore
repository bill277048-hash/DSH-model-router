# 依赖
node_modules/

# npm 锁文件：本项目用 **pnpm**（pnpm-lock.yaml 已入库），但 CI 的 test.yml
# 跑的是 `npm install`（为了贴近宿主安装方式）。任何人在本地复现 CI 都会生成
# package-lock.json —— 它既未被跟踪、原先也未被忽略，导致工作区持续 1 条脏状态，
# 容易被 `git add -A` 误提交。（2026-09-26 核实 ENG-02 时踩到）
package-lock.json

# 调试产物
*.log
npm-debug.log*
.DS_Store

# 本地备份（v0.9.6：不进版本库）
*.bak
*.bak.*
.bak-*

# 构建产物（可由源码重新打包；发布包存 NAS 归档）
DSH-model-router-v*.zip

# 例外：`assets/` 下的 zip 是 **Release 资产**（.github/workflows/release.yml
# 的 `files: assets/*.zip` 会在打 v* tag 时上传它们），必须入库。
# 注：上面那条规则本意只针对**根目录**的本地部署包，但无斜杠的模式会匹配任意层级，
# 故此处显式放开 assets/。2026-09-20 发现：远端 main（v0.8.1 时期）确实跟踪了
# assets/DSH-model-router-v0.8.0.zip —— 当时靠 `git add -f` 绕过，未记录原因。
!assets/*.zip

# 本机 Info-ZIP（Apple 版）在目标文件名含非 ASCII 时（如「…部署包.zip」）
# 会创建 ziXXXXXX 形式的临时文件且不自动清理。打包后请随手 rm，勿入库。
zi??????

# v0.9.14 新发现：`zip -r ... lib` 会在**被压缩目录内**留下 XXXXXXXX 形式的
# 临时副本（内容 = 被压缩文件本身，如 lib/XXMH38nm = metrics.js 的副本）。
# 两次踩坑（v0.9.10 / v0.9.14）都导致 `git add -A` 误入库。
# 打包后清理：`find . -name 'XX??????' -not -path './node_modules/*' -delete`
XX??????
